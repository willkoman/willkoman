export default function Library() {
  return (
    <div className="h-full overflow-auto p-8">
      <div className="max-w-3xl mx-auto">
        <h1 className="text-2xl font-bold mb-2">Library</h1>
        <p className="text-[var(--color-text-dim)] mb-6">
          Phase 1 will land: built-in templates (FPS / 3rd-person / strategy / emulation), recent files,
          and an "import from SteamInputDB URL" flow.
        </p>
        <div className="rounded-lg border border-[var(--color-border)] bg-[var(--color-panel)] p-6 text-sm text-[var(--color-text-dim)]">
          Nothing here yet.
        </div>
      </div>
    </div>
  );
}
